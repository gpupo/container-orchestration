#/bin/bash

PHP_SERVER=${PHP_SERVER:-php}
echo "upstream php-upstream { server ${PHP_SERVER}:9000; }" > /etc/nginx/conf.d/upstream.conf;
ln -s /etc/nginx/conf.d/upstream.conf /etc/nginx/sites-enabled/
